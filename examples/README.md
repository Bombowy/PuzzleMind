# Examples

Executable examples will be added with the milestone that implements their public
API. Planned examples include parsing a screenshot, inspecting a board snapshot,
running a Cats rule catalog, formatting deduction explanations, rendering debug
overlays, and dry-running guarded automation.

No example is provided in v0.1 because a script that only raises
`NotImplementedError` would suggest functionality that does not exist.
